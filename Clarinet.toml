[project]
name = "pyth-snapshot"
authors = ["PyTh Developer Team"]
description = "A decentralized goal tracking and verification system"
telemetry = false
requirements = []
[contracts.pyth-snapshot]
path = "contracts/pyth-snapshot.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
